# {{name}}

{{description}}

Generated with `create-notionless-plugin` (template: custom-callout).
Exercises: ctx.editor.registerBlock (a :::tip callout block that round-trips to Markdown)

Edit `index.js`, then reload from Settings → Plugins. See
`docs/PLUGIN_API_CONTRACT.md` for the full `ctx` API and the capability rules.
