# {{name}}

{{description}}

Generated with `create-notionless-plugin` (template: magic-login).
Exercises: ctx.auth.registerLoginMethod (alternate unlock of the same key)

Edit `index.js`, then reload from Settings → Plugins. See
`docs/PLUGIN_API_CONTRACT.md` for the full `ctx` API and the capability rules.
