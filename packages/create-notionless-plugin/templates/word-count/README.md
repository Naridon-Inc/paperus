# {{name}}

{{description}}

Generated with `create-notionless-plugin` (template: word-count).
Exercises `ctx.ui.statusItem` + `ctx.editor.onChange`.

Edit `index.js`, then reload from Settings → Plugins. See
`docs/PLUGIN_API_CONTRACT.md` for the full `ctx` API.
