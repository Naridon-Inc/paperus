# {{name}}

{{description}}

Generated with `create-notionless-plugin` (template: custom-section).
Exercises: ctx.ui.sidebarSection + ctx.ui.view + ctx.ui.navItem

Edit `index.js`, then reload from Settings → Plugins. See
`docs/PLUGIN_API_CONTRACT.md` for the full `ctx` API and the capability rules.
