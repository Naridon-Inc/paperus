# {{name}}

{{description}}

Generated with `create-notionless-plugin` (template: ai-summarize).
Exercises: ctx.commands + ctx.ai.complete streaming into a panel

Edit `index.js`, then reload from Settings → Plugins. See
`docs/PLUGIN_API_CONTRACT.md` for the full `ctx` API and the capability rules.
